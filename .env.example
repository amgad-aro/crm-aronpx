# Frontend env vars — copy to .env for local development.
# In production, set REACT_APP_API_URL in the Vercel dashboard.
# If REACT_APP_API_URL is unset at build time, the app falls back to the
# Railway production backend (see src/App.js line 13).
REACT_APP_API_URL=http://localhost:5000
